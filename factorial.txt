synchronized (std::ignore) {
  std::cout << "Hello, world!" << std::endl;
  case 0.0f of {
    _ -> use strict qw/++i/;
  }
  goto nullptr;
  std::cout << "Hello, world!" << std::endl;
  goto __dict__;
  (format t "
    WHILE $ARGV > $[
      s/*read-eval*//g
    WEND
  ")
  proc <> { `uniq -c` } {
    OUTPUT = Object
  }
}
